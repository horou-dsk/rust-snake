fn main() {
    // let mut v = vec![[1, 2], [3, 4]];
    // // v.iter_mut().for_each(|x| {
    // //     *x += 2;
    // // });
    // let head = &mut v[0];
    // head[0] -= 1;
    // v.insert(0, [5, 1]);
    // println!("{:?}", v);
    println!("{}", rand::random::<u8>() % 30)
}